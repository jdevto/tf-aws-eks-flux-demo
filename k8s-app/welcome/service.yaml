# Welcome page service
apiVersion: v1
kind: Service
metadata:
  name: welcome
  namespace: default
  labels:
    app: welcome
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: welcome

